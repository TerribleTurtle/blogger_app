<h1>Articles</h1>

<%= link_to "Create New Article", new_article_path, class: 'article_title' %>

<ul id="articles">
	<% @articles.each do |article| %>
		<li><%= link_to article.title, article_path(article) %></li>
		<li><%= article.body %></li>
	<% end %>

</ul>